apiVersion: v1
kind: Service
metadata:
  name: payments-go
  labels:
    app: payments-go
spec:
  type: ClusterIP
  selector:
    app: payments-go
    release: {{ .Release.Name }}
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
