apiVersion: v1
kind: Service
metadata:
  name: web-legacy
  labels:
    app: web-legacy
spec:
  type: ClusterIP
  selector:
    app: web-legacy
    release: {{ .Release.Name }}
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
